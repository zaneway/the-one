---
description: theone 记忆捕获：关键字段完备，content_summary 使用结构化索引卡
alwaysApply: true
---

# The One：memory_observe 捕获规则

目标：在有长期价值或检索价值时写入 `raw_event`，保证自动抽取和后续 `memory.context` 注入所需的关键字段完备。禁止只传 `event_type` + 一句自由文本 `content_summary`。

结构化 `content_summary` 的单一规范见：`doc/shared/content-summary-structured.md`。本 Rule 只维护 Cursor 端调用约束，不维护另一套摘要标准。

`content_summary` 负责检索索引卡；需要保留原始事实时使用 `raw_payload_json`。当前默认不脱敏，`redaction_state=raw`；服务端默认 raw payload 上限为 1MiB，应尽量不截断。

`memory_observe` 只保存 `raw_event`，不要求也不等待外部 AI 在写入前做语义简化。后续自动链路从已落库的 `raw_event` 抽取 evidence / candidate，再决定是否晋升为 `memory_item`。

> Rule 不能代替系统 Hook；须 Agent 模式 + MCP `theone` 已连接 + 仓库根打开。

## 1. 线程级常量

| 字段 | 值 |
|------|-----|
| `agent_type` | `cursor` |
| `workspace_id` | `local_default_workspace` |
| `project_id` | 默认取 Hook payload 中当前对话工作目录名；可用 `THEONE_PROJECT_ID` 覆盖 |
| `repo_id` | 默认同 `project_id`；可用 `THEONE_REPO_ID` 覆盖 |
| `source_channel` | `agent_session` |
| `session_id` | Hook 建立的 binding 中的值；手动调用时可自命名 `sess_cursor_<YYYYMMDD>_<topic>` |
| `task_id` | Hook 建立的 task 值；手动调用时可用 `task_<topic>`，全线程不变 |

`session_id` 必填。不要用 `memory_observe` 主动写 `session.start`。

## 2. L1：每次 observe 必填

缺少任一项时不要调用，先补全再提交。

| 字段 | 要求 |
|------|------|
| `event_type` | 合法枚举值 |
| `actor` | `user` / `agent` / `tool` / `adapter` |
| `agent_type` | `cursor` |
| `workspace_id` | `local_default_workspace` |
| `project_id` / `repo_id` | 默认取 Hook payload 中当前对话工作目录名；必要时用 `THEONE_PROJECT_ID` / `THEONE_REPO_ID` 覆盖 |
| `source_channel` | `agent_session` |
| `content_summary` | 非空结构化索引卡，建议 200-800 字，硬上限 6000 |

## 2.1 raw_payload_json

对高价值事件建议携带 `raw_payload_json`：

| 字段 | 要求 |
|------|------|
| `raw_payload_json` | JSON 字符串，保存原始/准原始事实；不要放到 `source_refs` |
| `payload_schema` | 如 `turn.completed.v1`、`tool_result.v1`、`file_edit.v1` |
| `raw_payload_hash` | 可省略，服务端会自动计算 |
| `redaction_state` | 当前填 `raw` |
| `truncation` | 未截断时 `{ "truncated": false }`；超过上限才标记原因 |

建议：

- `turn.completed`：保存同一轮用户请求和 Agent 应答的原文或准原文。
- `user.declaration` / `user.correction`：仅在需要单独记录高优先级声明/纠正时保存用户原文或准原文。
- `conversation.message` / `agent.response.summary`：仅用于兼容旧的单边消息路径。
- 普通工具结果由 Hook 控制面处理，不要求 Agent 主动写 `tool.result.summary`；只有失败根因、重复错误、关键测试结论值得长期复用时，才用 `turn.completed`、`agent.decision`、`user.declaration` 或 `memory_remember` 记录结论。
- `file.edit.summary`：系统 Hook 可产生该事件用于链路诊断，但默认不进入 `raw_event`；Agent 不要通过 MCP 主动补写文件路径、hash、change_type 或 diff 摘要。

## 3. 结构化 content_summary

必须遵守 `doc/shared/content-summary-structured.md`：

- `content_summary` 使用固定标签：`【事件】`、`【事实】`、`【结论/决策】`、`【约束】`、`【关联】`、`【状态】`。
- 无内容的标签段可省略。
- 高价值信息必须前置；超过 400 字时，前 400 字内必须出现 `【结论` 或 `【约束】`。
- 禁止对话复述、代码块、`full_diff`、长日志、流水账，禁止为填满 6000 写长文。
- `salient_spans` 写 1-5 条原子事实，每条 <=500 字，不得粘贴全文。
- `keywords` 写 3-8 个检索锚点。
- `memory_remember.content` 只写长期结论，建议 <=1500 字。

当前 `memory.context` 仍可能头部截断；结构化索引卡的目的，是缩小入库体积并把高价值信息放到截断前部。

## 4. L2：对话/结论类事件必填

适用于：`turn.completed`、`conversation.message`、`user.*`、`agent.response.summary`、`agent.decision`、`task.result`。

| 字段 | 要求 |
|------|------|
| `keywords` | 3-8 个有区分度的检索词 |
| `salient_spans` | 1-5 条短句，每条 <=500 字 |
| `source_refs` | 至少 1 条，含 `source_type` + `capture_method` |
| `retention_hint` | `short_term` / `long_term` |
| `session` | `{ "goal_summary": "...", "status": "active|completed|..." }` |
| `task` | `{ "task_summary": "...", "status": "...", "outcome_summary": "..." }` |

## 5. L3：按事件类型额外必填

| event_type | 额外必填 |
|------------|----------|
| `turn.completed` | `input_summary` 保存用户原始请求；`output_summary` 保存 Agent 原始应答 |
| `file.edit.summary` | Hook 诊断事件，默认不写 `raw_event` |
| `session.end` | `session.status=completed`；`task.outcome_summary` 非空 |

可省略：非工具且非 `turn.completed` 事件的 `tool_name` / `input_summary` / `output_summary`；`content_hash`；`occurred_at`。

## 6. source_refs 最低规范

对话/分析类：

```json
[{ "source_type": "agent_session", "capture_method": "cursor_rule", "session_id": "<session_id>" }]
```

文件修改信息不要进入 `raw_event`；禁止通过 `source_refs` 或 `raw_payload_json` 保存 `file_path`、`change_type`、hash 或 `full_diff`。普通工具输出不要主动 observe；高价值工具结论写入结构化摘要，不贴完整输出。

Agent 通过 MCP 主动调用 `memory_observe` 时，`source_refs.capture_method` 使用 `manual_mcp_call` 或 `cursor_rule`；系统 Hook 采集的事件由 Driver 自动写入 `producer=<agent>_hook:<hook>`，不要在 MCP payload 中伪造 hook producer。

## 7. 何时调用

| 时机 | event_type |
|------|------------|
| 用户提出新问题/约束并完成本轮应答 | `turn.completed` |
| 仅记录用户高优先级纠正/声明 | `user.correction` / `user.declaration` |
| 兼容旧路径的单边用户消息 | `conversation.message` |
| 兼容旧路径的单边助手摘要 | `agent.response.summary` |
| 改了代码 | 汇入本轮 `turn.completed` 的结果/验证；架构取舍写 `agent.decision`；不要单独写 `file.edit.summary` |
| 架构/取舍结论 | `agent.decision` |
| 用户表示结束 | `session.end` |

可不调用：纯“好/继续”且无新信息；用户明确“不要记录”。

有实质内容的任务优先记录本轮 `turn.completed`。不要把同一轮用户请求和助手应答拆成 `conversation.message` + `agent.response.summary` 两条 raw_event。

## 8. 示例

```json
{
  "session_id": "sess_cursor_20260603_structured_summary",
  "task_id": "task_structured_summary",
  "agent_type": "cursor",
  "workspace_id": "local_default_workspace",
  "project_id": "the-one",
  "repo_id": "the-one",
  "event_type": "turn.completed",
  "source_channel": "agent_session",
  "actor": "adapter",
  "input_summary": "用户要求三端 Agent 将 content_summary 从自由文本改为结构化索引卡。",
  "output_summary": "本轮已采用结构化索引卡规范记录。",
  "content_summary": "【约束】不新增 DB 字段，不修改 search_text 拼接，不修改 memory.context 截断策略。\n【事件】用户要求三端 Agent 将 content_summary 从自由文本改为结构化索引卡。\n【结论/决策】本轮已采用结构化索引卡规范记录。\n【关联】doc/shared/content-summary-structured.md",
  "raw_payload_json": "{\"user\":{\"message\":\"用户要求三端 Agent 将 content_summary 从自由文本改为结构化索引卡。\"},\"agent\":{\"message\":\"本轮已采用结构化索引卡规范记录。\"}}",
  "payload_schema": "turn.completed.v1",
  "redaction_state": "raw",
  "truncation": { "truncated": false },
  "keywords": ["content_summary", "structured", "memory_observe", "cursor", "memory.context"],
  "salient_spans": [
    "三端 Agent 的 content_summary 必须结构化",
    "结构化标准以 doc/shared/content-summary-structured.md 为单一真相"
  ],
  "source_refs": [
    { "source_type": "agent_session", "capture_method": "cursor_rule", "session_id": "sess_cursor_20260603_structured_summary" },
    { "source_type": "user_prompt", "capture_method": "cursor_rule" },
    { "source_type": "agent_response", "capture_method": "cursor_rule" }
  ],
  "retention_hint": "short_term",
  "session": { "goal_summary": "统一三端结构化 content_summary", "status": "active" },
  "task": { "task_summary": "实现结构化摘要捕获规范", "status": "active", "outcome_summary": "" }
}
```

## 9. 与 memory_remember 的分工

| 场景 | 工具 |
|------|------|
| 高价值过程轨迹、对话摘要、失败根因 | `memory_observe` |
| 应长期保留的稳定结论 | 额外 `memory_remember` |
| 新任务前召回 | `memory_search` / `memory_context` |

`memory_remember.content` 只写长期可复用内容，不复制本轮过程；建议 <=1500 字。
